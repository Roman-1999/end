<br>
<p id="notice"><%= notice %></p>

			<div id="ed">
				<% if current_user != nil and current_user.admin or current_user.moderator %>
						<%= link_to "Edit",  edit_pet_path %> or
						<%= link_to "Delete", pet_path, data: {confirm: 'Точно видалити данний пост?'}, method: :delete %>
				<%end%>
			</div>

<p>
  <strong>Image:</strong>
  <p><%= image_tag @pet.image.url %></p>
</p>
 
<p>
  <strong>Name:</strong>
  <%= @pet.name %>
</p>
 
<p>
  <strong>Description:</strong>
  <%= @pet.description %>
</p>
  
<%= link_to 'Edit', edit_pet_path(@pet) %> |
<%= link_to 'Back', pets_path %>